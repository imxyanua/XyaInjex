"""Template engine parser-divergence regression cases."""

from __future__ import annotations

from .models import CorpusCase

TEMPLATE_DIALECTS = [
    "jinja2",
    "twig",
    "liquid",
    "nunjucks",
    "freemarker",
    "erb",
    "handlebars",
    "velocity",
    "mako",
    "blade",
    "razor",
    "gotemplate",
    "ejs",
    "thymeleaf",
]

TEMPLATE_CASES: tuple[CorpusCase, ...] = (
    CorpusCase(
        id="jinja-mustache-hello",
        template="Hello {{ {INPUT} }}",
        payload="{{7*7}}",
        note="Mustache-style {{ }} SSTI; freemarker, erb, velocity, and mako disagree.",
        divergent=True,
    ),
    CorpusCase(
        id="jinja-config-access",
        template="Hello {{ {INPUT} }}",
        payload="{{ config }}",
        note="Config/object access via double-brace syntax.",
        divergent=True,
    ),
    CorpusCase(
        id="freemarker-dollar",
        template="{{ {INPUT} }}",
        payload="${7*7}",
        note="Dollar-brace math is native to freemarker/velocity/mako but not erb.",
        divergent=True,
    ),
    CorpusCase(
        id="hash-expression",
        template="{{ {INPUT} }}",
        payload="#{7*7}",
        note="Hash-curly expression syntax accepted unevenly across engines.",
        divergent=True,
    ),
    CorpusCase(
        id="set-tag-handlebars-only",
        template="{% set x = '{INPUT}' %}",
        payload="{{7*7}}",
        note="Only handlebars treats the follow-on mustache as top-level code here.",
        divergent=True,
    ),
    CorpusCase(
        id="single-quote-breakout",
        template="{{ '{INPUT}' }}",
        payload="'}{{7*7}}",
        note="Break out of a quoted mustache string.",
        divergent=True,
    ),
    CorpusCase(
        id="raw-block-breakout",
        template="{% raw %}{INPUT}{% endraw %}",
        payload="{{7*7}}",
        note="Escape a raw block and inject mustache code afterward.",
        divergent=True,
    ),
    CorpusCase(
        id="tag-logic-inject",
        template="{{ {INPUT} }}",
        payload="{% if 1 %}x{% endif %}",
        note="Inject template logic tags after a mustache sink.",
        divergent=True,
    ),
    CorpusCase(
        id="filter-safe-bypass",
        template="{{ {INPUT} | safe }}",
        payload="{{7*7}}",
        note="Expression inside a filtered mustache slot.",
        divergent=True,
    ),
    CorpusCase(
        id="freemarker-bracket-dollar",
        template="[[{INPUT}]]",
        payload="${7*7}",
        note="Freemarker bracket refs vs mustache engines on dollar syntax.",
        divergent=True,
    ),
    CorpusCase(
        id="plain-text-literal",
        template="Hello {INPUT}",
        payload="world",
        note="Plain text substitution with no template metacharacters.",
        divergent=False,
    ),
    CorpusCase(
        id="raw-block-contained",
        template="{% raw %}{INPUT}{% endraw %}",
        payload="hello",
        note="Payload stays inside a raw block on every engine.",
        divergent=False,
    ),
    CorpusCase(
        id="bracket-literal",
        template="[[{INPUT}]]",
        payload="value",
        note="Freemarker bracket text; thymeleaf treats [[ ]] as an inline expression.",
        divergent=True,
    ),
    CorpusCase(
        id="razor-at-expression",
        template="Hello {INPUT}",
        payload="@(7*7)",
        note="Razor @(...) expression syntax is ignored by other engines.",
        divergent=True,
    ),
    CorpusCase(
        id="razor-at-block",
        template="Hello {INPUT}",
        payload="@{7*7}",
        note="Razor @{...} code block syntax is razor-only.",
        divergent=True,
    ),
    CorpusCase(
        id="blade-unescaped-slot",
        template="{!! {INPUT} !!}",
        payload="{{7*7}}",
        note="Blade {!! !!} raw slots accept mustache-style follow-on code.",
        divergent=True,
    ),
    CorpusCase(
        id="blade-raw-in-mustache",
        template="{{ {INPUT} }}",
        payload="{!!7*7!!}",
        note="Blade raw echo tags inside a mustache sink.",
        divergent=True,
    ),
    CorpusCase(
        id="ejs-output-tag",
        template="Hello {INPUT}",
        payload="<%= 7*7 %>",
        note="EJS/ERB-style output tags; jinja-style engines stay plain text.",
        divergent=True,
    ),
    CorpusCase(
        id="thymeleaf-bracket-expr",
        template="[[{INPUT}]]",
        payload="7*7",
        note="Thymeleaf [[ ]] inline expressions; freemarker sees bracket text.",
        divergent=True,
    ),
    CorpusCase(
        id="thymeleaf-paren-expr",
        template="[( {INPUT} )]",
        payload="7*7",
        note="Thymeleaf [( )] expression syntax is thymeleaf-only.",
        divergent=True,
    ),
    CorpusCase(
        id="gotemplate-field",
        template="Hello {INPUT}",
        payload="{{.Cmd}}",
        note="Go template field access; only gotemplate and mustache engines parse it.",
        divergent=True,
    ),
    CorpusCase(
        id="razor-code-sink",
        template="@({INPUT})",
        payload="7*7",
        note="Payload lands inside a Razor @(...) expression sink.",
        divergent=True,
    ),
    CorpusCase(
        id="blade-unescaped-literal",
        template="{!! {INPUT} !!}",
        payload="7*7",
        note="Plain text inside a Blade unescaped slot stays contained elsewhere.",
        divergent=True,
    ),
    CorpusCase(
        id="thymeleaf-inline-bracket",
        template="Hello [[{INPUT}]]",
        payload="7*7",
        note="Thymeleaf inline [[ ]] attribute-style expression.",
        divergent=True,
    ),
)
